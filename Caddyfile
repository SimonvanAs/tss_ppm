# Use environment variable DOMAIN for the site address
# For localhost, Caddy will use HTTP. For a real domain, it auto-enables HTTPS.
{$DOMAIN:localhost} {
    # Reverse proxy to frontend nginx
    reverse_proxy frontend:80

    # Enable compression
    encode gzip

    # Security headers
    header {
        X-Content-Type-Options nosniff
        X-Frame-Options DENY
        Referrer-Policy strict-origin-when-cross-origin
    }

    # Logging
    log {
        output stdout
        format console
    }
}
